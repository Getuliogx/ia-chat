{
  "name": "carolia-icarolinaporto-sem-obs",
  "version": "4.0.0",
  "private": true,
  "description": "CarolIA: Twitch EventSub no Render + painel de personalidade + IA nativa do StreamElements, sem OBS.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check public/app.js"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "dotenv": "^16.6.1",
    "express": "^5.1.0",
    "ws": "^8.18.3"
  }
}
